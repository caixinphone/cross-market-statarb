# Copy to config/secrets.yaml (gitignored) and fill in, OR set the equivalent
# environment variables ALPACA_API_KEY_ID / ALPACA_API_SECRET_KEY.
# Paper-trading data keys are sufficient (free, IEX feed). Get them at
# https://app.alpaca.markets -> Paper Trading -> API Keys.
alpaca_api_key_id: YOUR_KEY_ID
alpaca_api_secret_key: YOUR_SECRET_KEY
